# AI服务后端开发任务清单

**文档版本**: v1.0\
**创建日期**: 2026-04-04\
**基于**: 测试报告问题分析 + 后端对接梳理

***

## 一、待开发功能任务

### 1.1 高优先级任务 (P0)

| 编号 | 任务名称 | 涉及文件 | 工作量 | 状态 |
| --- | --- | --- | --- | --- |
| T1 | 病历生成健壮性重构 | `service.py` | 4h | ⬜ 待开发 |
| T2 | 危急情况识别与警告 | `service.py` | 3h | ⬜ 待开发 |
| T3 | 病症识别同义词库 | `service.py`, `test_streaming.py` | 2h | ⬜ 待开发 |
| T4 | 专业性术语强制检查 | `service.py`, `prompts.py` | 2h | ⬜ 待开发 |

### 1.2 中优先级任务 (P1)

| 编号 | 任务名称 | 涉及文件 | 工作量 | 状态 |
| --- | --- | --- | --- | --- |
| T5 | 场景化建议模板 | `prompts.py` | 3h | ⬜ 待开发 |
| T6 | API重试与超时机制 | `service.py` | 2h | ⬜ 待开发 |
| T7 | 响应缓存机制 | `service.py` | 2h | ⬜ 待开发 |
| T8 | 性能监控模块 | `service.py` | 2h | ⬜ 待开发 |

### 1.3 低优先级任务 (P2)

| 编号 | 任务名称 | 涉及文件 | 工作量 | 状态 |
| --- | --- | --- | --- | --- |
| T9 | 测试评估标准优化 | `test_streaming.py` | 2h | ⬜ 待开发 |
| T10 | 日志系统完善 | `service.py`, `server.py` | 1h | ⬜ 待开发 |

***

## 二、任务详细说明

### T1: 病历生成健壮性重构 ⭐⭐⭐

**问题描述**: 第14轮测试病历内容为空，解析逻辑脆弱

**需要做的事**:

1. **数据验证层**
   - [ ] 添加会话存在性检查
   - [ ] 添加消息记录非空检查
   - [ ] 添加对话内容提取函数

2. **解析增强层**
   - [ ] 支持多种格式解析（【字段】、字段:、字段：）
   - [ ] 添加正则表达式容错
   - [ ] 处理JSON解析异常

3. **降级方案层**
   - [ ] 实现 `_generate_fallback_report()` 函数
   - [ ] 从对话中提取症状作为降级
   - [ ] 生成基础病历模板

4. **错误处理层**
   - [ ] 每个步骤添加try-catch
   - [ ] 记录详细错误日志
   - [ ] 返回有意义的错误信息

**代码位置**: `zhipuGLM/service.py` - `generate_medical_report()` 函数

---

### T2: 危急情况识别与警告 ⭐⭐⭐

**问题描述**: 患者安全建议合格率0%，危急情况未特殊处理

**需要做的事**:

1. **危急情况关键词库**
   ```python
   CRITICAL_CONDITIONS = {
       "high_fever": ["发烧.*3[89]", "高烧", "体温.*39"],
       "wound_infection": ["伤口.*流脓", "化脓", "红肿.*发热"],
       "severe_bleeding": ["出血.*不止", "大量出血"],
       "severe_pain": ["剧烈.*疼痛", "疼痛.*难忍"]
   }
   ```

2. **识别函数**
   - [ ] 实现 `detect_critical_condition(user_input)` 函数
   - [ ] 使用正则表达式匹配
   - [ ] 返回危急类型和警告信息

3. **强制警告机制**
   - [ ] 在对话流程中优先检测危急情况
   - [ ] 危急情况强制返回紧急警告
   - [ ] 添加安全建议模板

4. **安全建议模板**
   - [ ] 高烧场景建议
   - [ ] 伤口感染场景建议
   - [ ] 出血场景建议
   - [ ] 剧痛场景建议

**代码位置**: `zhipuGLM/service.py` - `chat_stream()` 函数开头

---

### T3: 病症识别同义词库 ⭐⭐⭐

**问题描述**: 病症识别率0%-33%，评估方式死板

**需要做的事**:

1. **同义词库建设**
   ```python
   SYMPTOM_SYNONYMS = {
       "伤口发炎": ["伤口发炎", "伤口感染", "红肿", "发热感", "化脓", "流脓"],
       "发烧": ["发烧", "发热", "体温升高", "低烧", "高烧"],
       "疼痛": ["疼痛", "疼", "痛", "不适", "难受"],
       "恶心": ["恶心", "想吐", "呕吐感", "反胃"],
       "出血": ["出血", "渗血", "流血", "有血"]
   }
   ```

2. **识别函数**
   - [ ] 实现 `extract_symptoms(text)` 函数
   - [ ] 遍历同义词库匹配
   - [ ] 返回标准化症状列表

3. **测试评估优化**
   - [ ] 修改测试脚本使用同义词匹配
   - [ ] 计算识别率时考虑同义词

**代码位置**: `zhipuGLM/service.py` + `tests/test_streaming.py`

---

### T4: 专业性术语强制检查 ⭐⭐⭐

**问题描述**: AI建议专业性评分0%，提示词未要求专业术语

**需要做的事**:

1. **专业术语库**
   ```python
   PROFESSIONAL_TERMS = {
       "感染相关": ["感染", "炎症", "化脓", "细菌", "抗生素"],
       "伤口护理": ["换药", "消毒", "清创", "缝合", "愈合"],
       "症状描述": ["发热", "疼痛", "红肿", "渗出", "出血"],
       "就医建议": ["就医", "复查", "急诊", "门诊", "住院"],
       "风险警示": ["立即", "紧急", "危险", "并发症", "恶化"]
   }
   ```

2. **提示词优化**
   - [ ] 修改 `CHAT_SYSTEM_PROMPT` 强制使用专业术语
   - [ ] 添加输出格式要求
   - [ ] 添加专业性评分标准

3. **自动检查函数**
   - [ ] 实现 `check_professionalism(response)` 函数
   - [ ] 检查是否包含专业术语
   - [ ] 返回专业性评分

**代码位置**: `zhipuGLM/prompts/prompts.py` + `zhipuGLM/service.py`

---

### T5: 场景化建议模板 ⭐⭐

**问题描述**: 健康建议质量评分0%，建议模板不匹配场景

**需要做的事**:

1. **场景分级**
   ```python
   SCENARIOS = {
       "high_severity": ["发烧>38.5", "伤口流脓", "剧烈疼痛", "出血不止"],
       "medium_severity": ["伤口渗血", "轻微红肿", "低烧<38.5"],
       "low_severity": ["饮食咨询", "恢复咨询", "轻微不适"]
   }
   ```

2. **建议模板**
   - [ ] 高危场景模板（紧急警告格式）
   - [ ] 中危场景模板（观察建议格式）
   - [ ] 低危场景模板（日常建议格式）

3. **场景识别函数**
   - [ ] 实现 `assess_severity(symptoms)` 函数
   - [ ] 根据症状评估严重程度
   - [ ] 返回对应场景级别

**代码位置**: `zhipuGLM/prompts/prompts.py`

---

### T6: API重试与超时机制 ⭐⭐

**问题描述**: 响应时间波动大(13s-55s)，无重试机制

**需要做的事**:

1. **重试装饰器**
   ```python
   @retry_on_failure(max_retries=3, delay=1, backoff=2)
   def call_zhipu_api(messages):
       ...
   ```

2. **超时处理**
   - [ ] 添加API调用超时设置
   - [ ] 超时后返回友好错误信息
   - [ ] 记录超时日志

3. **错误恢复**
   - [ ] API错误时返回降级响应
   - [ ] 提示用户稍后重试

**代码位置**: `zhipuGLM/service.py`

---

### T7: 响应缓存机制 ⭐⭐

**问题描述**: 重复查询浪费资源

**需要做的事**:

1. **缓存管理器**
   ```python
   class ResponseCache:
       def __init__(self, max_size=100, ttl=3600):
           self.cache = {}
           self.max_size = max_size
           self.ttl = ttl
   ```

2. **缓存键生成**
   - [ ] 基于用户输入+上下文生成缓存键
   - [ ] 使用MD5哈希

3. **缓存策略**
   - [ ] 相同问题返回缓存结果
   - [ ] 设置TTL过期时间
   - [ ] LRU淘汰策略

**代码位置**: `zhipuGLM/service.py`

---

### T8: 性能监控模块 ⭐⭐

**问题描述**: 无法定位性能瓶颈

**需要做的事**:

1. **监控指标**
   - [ ] API调用耗时
   - [ ] RAG检索耗时
   - [ ] 病历生成耗时
   - [ ] 端到端响应时间

2. **监控类**
   ```python
   class PerformanceMonitor:
       def record(self, operation, duration):
           ...
       def get_stats(self, operation):
           ...
       def check_anomaly(self, operation, duration):
           ...
   ```

3. **异常检测**
   - [ ] 响应时间超过阈值告警
   - [ ] 记录异常日志

**代码位置**: `zhipuGLM/service.py`

---

### T9: 测试评估标准优化 ⭐

**问题描述**: 测试评估过于严格

**需要做的事**:

1. **同义词匹配**
   - [ ] 使用同义词库进行匹配
   - [ ] 计算语义相似度

2. **评分标准调整**
   - [ ] 降低关键词匹配要求
   - [ ] 增加语义理解评分

**代码位置**: `tests/test_streaming.py`

---

### T10: 日志系统完善 ⭐

**问题描述**: 日志不够详细

**需要做的事**:

1. **日志级别**
   - [ ] DEBUG: 详细调试信息
   - [ ] INFO: 关键流程信息
   - [ ] WARNING: 警告信息
   - [ ] ERROR: 错误信息

2. **日志内容**
   - [ ] 请求参数
   - [ ] 响应内容
   - [ ] 耗时统计
   - [ ] 错误堆栈

**代码位置**: `zhipuGLM/service.py`, `connect/server.py`

***

## 三、开发顺序建议

```
第1天: T1(病历生成) + T2(危急情况)
第2天: T3(同义词库) + T4(专业性)
第3天: T5(场景模板) + T6(重试机制)
第4天: T7(缓存) + T8(监控)
第5天: T9(测试优化) + T10(日志) + 测试验证
```

***

## 四、验收标准

| 任务 | 验收标准 |
| --- | --- |
| T1 | 病历生成成功率 ≥ 95%，字段完整性 100% |
| T2 | 危急情况识别率 ≥ 90%，安全建议合格率 ≥ 80% |
| T3 | 病症识别率 ≥ 80% |
| T4 | 专业性评分 ≥ 80% |
| T5 | 建议质量评分 ≥ 60% |
| T6 | API调用成功率 ≥ 99% |
| T7 | 缓存命中率 ≥ 30%（重复场景） |
| T8 | 监控数据完整，异常检测有效 |
| T9 | 测试通过率 ≥ 95% |
| T10 | 日志完整，可追溯问题 |

***

## 五、文件修改清单

| 文件 | 修改任务 | 优先级 |
| --- | --- | --- |
| `zhipuGLM/service.py` | T1, T2, T3, T4, T6, T7, T8, T10 | 高 |
| `zhipuGLM/prompts/prompts.py` | T4, T5 | 高 |
| `tests/test_streaming.py` | T3, T9 | 中 |

***

**文档编写**: AI Assistant\
**更新日期**: 2026-04-04
